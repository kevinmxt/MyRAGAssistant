# RAG 增强需求待办

2026-07-30 首次梳理，共 6 个方向；2026-07-31 补充重排序，共 7 个方向，按管线顺序排列：

1. **文档切割策略增强和调整** — 优化分块算法，提升检索片段质量 ✅
2. **向量化增强** — 提升嵌入表示质量 ✅
3. **引入向量数据库** — 从内存+JSON 迁移到外部向量数据库 ✅
4. **多路召回机制** — 关键词+向量混合检索
5. **重排序增强（Re-rank）** — 对粗召回结果进行精细重排序，提升 Top-K 精度
6. **自动化量化 RAG 效果** — 建立评估基线和自动化评测流程
7. **提示词增强** — 降低模型幻觉

## 依赖关系（初步）

```
(1) 文档切割 ──┐
               │
(2) 向量化增强 ─┼──→ (4) 多路召回 ──→ (5) 重排序 ──→ (6) 效果评估
               │
(3) 向量数据库 ─┘
               │
(7) 提示词增强 ───────────────────────────────────────┘
```

(1)(2)(3) 相对独立，可并行推进；(4) 依赖 (1)(2) 的效果作为基线；(5) 重排序在 (4) 粗召回之后，对候选集精排；(6) 是衡量一切的尺子；(7) 贯穿始终。
